# Normalize line endings to LF in the repo and on checkout.
# Prevents CRLF-related tool noise (formatters, linters, diffs) on Windows.
* text=auto eol=lf

# Binary assets must never be normalized.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
